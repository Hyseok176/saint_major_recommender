feat: Implement persistent course cart with dedicated table

Implemented a persistent, user-specific course cart by creating a new dedicated table (`saved_courses`) to store the items. This aligns with the explicit requirement for a separate table for this functionality.

Key changes:
- Created new `SavedCourse` entity and `SavedCourseRepository`.
- Implemented service and controller logic to manage cart items via the `/api/saved-courses` endpoints.
- Replaced all client-side `localStorage` cart logic in `all-courses.html`, `recommend.html`, and `results.html` with API calls to the backend.
- The floating cart is now populated with data from the database, ensuring persistence across sessions.
- The `results.html` page is also updated to display the saved items correctly.